# Golangci-lint configuration for Raioz project
# Strict rules for code quality and consistency

run:
  timeout: 5m
  tests: true
  build-tags: ""

linters-settings:
  # Maximum line length
  lll:
    line-length: 120
    tab-width: 1

  # Function complexity
  funlen:
    lines: 100
    statements: 50

  # File length check (via custom linter or gocritic)
  gocritic:
    enabled-tags:
      - diagnostic
      - experimental
      - opinionated
      - performance
      - style
    disabled-checks:
      - dupImport # allow duplicate imports if needed

  # Cyclomatic complexity
  gocyclo:
    min-complexity: 15

  # Error handling
  errcheck:
    check-type-assertions: true
    check-blank: true
    exclude-functions:
      - (io.Closer).Close
      - (*bytes.Buffer).Write

  # Code formatting
  gofmt:
    simplify: true

  # Import ordering
  goimports:
    local-prefixes: raioz

  # Unused code
  unused:
    check-exported: false

  # Style issues
  stylecheck:
    checks: ["all"]

  # Naked returns
  nakedret:
    max-func-lines: 30

  # Predeclared identifiers
  predeclared:
    ignore: ""

  # Whitespace
  whitespace:
    multi-if: false
    multi-func: false

linters:
  enable:
    # Default linters
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports

    # Code quality
    - gocritic
    - gocyclo
    - funlen
    - predeclared
    - stylecheck
    - copyloopvar
    - exhaustive
    - err113

    # Style
    - misspell
    - revive
    - unconvert
    - unparam
    - whitespace
    - wrapcheck

    # Performance
    - bodyclose
    - noctx
    - rowserrcheck

    # Security
    - gosec

    # Custom
    - lll # Line length linter

issues:
  exclude-dirs:
    - vendor
    - node_modules
  exclude-files:
    - ".*\\.pb\\.go$"
    - ".*\\.generated\\.go$"

  # Maximum issues count per one linter
  max-issues-per-linter: 0
  max-same-issues: 0

  # Exclude patterns
  exclude-rules:
    # Exclude some linters from generated files
    - path: _test\.go
      linters:
        - errcheck
        - funlen
        - gocyclo

    # Allow long lines in test files
    - path: _test\.go
      linters:
        - lll

    # Allow TODO comments
    - text: "TODO"
      linters:
        - gosec

  new: false

output:
  formats:
    - format: colored-line-number
  print-issued-lines: true
  print-linter-name: true
