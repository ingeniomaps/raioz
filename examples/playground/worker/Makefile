.PHONY: dev run stop

dev:
	@echo "Worker started in dev mode (processing jobs...)"
	@while true; do echo "[worker] polling for jobs..."; sleep 5; done

run:
	@echo "Worker started"
	@while true; do sleep 5; done

stop:
	@echo "Worker stopped"
